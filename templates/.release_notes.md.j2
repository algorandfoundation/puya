# {{ version.as_tag() }} ({{ release.tagged_date.strftime("%Y-%m-%d") }})
{% for type_, commits in release["elements"] | dictsort %}{% if type_ in ("feat", "fix", "perf") %}
## {{ type_ | capitalize }}
{% for commit in commits %}
* {{ commit.message.rstrip() | indent(2)}} ([`{{ commit.short_hash }}`]({{ commit.hexsha | commit_hash_url }}))
{% endfor %}{% endif %}{% endfor %}
