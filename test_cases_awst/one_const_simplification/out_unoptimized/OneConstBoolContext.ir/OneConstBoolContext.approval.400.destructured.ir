main LLMGenerated::OneConstBoolContext.approvalProgram:
    block@0: // L1
        let tmp%0#0: uint64 = (txn NumAppArgs)
        let x#0: uint64 = tmp%0#0
        let tmp%1#0: uint64 = (* 1u x#0)
        (assert tmp%1#0) // mul a_const==1 -> RIGHT
        let tmp%2#0: uint64 = (* x#0 1u)
        (assert tmp%2#0) // mul b_const==1 -> LEFT
        let tmp%3#0: uint64 = (/ x#0 1u)
        (assert tmp%3#0) // div_floor b_const==1 -> LEFT
        let tmp%4#0: uint64 = (+ 0u x#0)
        (assert tmp%4#0) // add a_const==0 -> RIGHT
        let tmp%5#0: uint64 = (+ x#0 0u)
        (assert tmp%5#0) // add b_const==0 -> LEFT
        let tmp%6#0: uint64 = (- x#0 0u)
        (assert tmp%6#0) // sub b_const==0 -> LEFT
        let tmp%7#0: uint64 = (* x#0 0u)
        (assert tmp%7#0) // mul 0 -> 0
        let tmp%8#0: uint64 = (% x#0 1u)
        (assert tmp%8#0) // mod b_const==1 -> 0
        let tmp%9#0: uint64 = (&& x#0 0u)
        (assert tmp%9#0) // and_ 0 -> 0
        return 1u