contract BooleanBinaryOps
{
  method_resolution_order: (
    algopy._base_contract.BaseContract,
  )
  
  subroutine test_cases.numeric_upcasts.contract.BooleanBinaryOps.approval_program(): bool
  {
    boolean: bool = false
    uint64: uint64 = 3u
    biguint: biguint = 5n
    assert(reinterpret_cast<uint64>(boolean) * uint64 == 0u)
    assert(itob(reinterpret_cast<uint64>(boolean)) b* biguint == 0n)
    assert(reinterpret_cast<uint64>(boolean) * 3u == 0u)
    assert(itob(reinterpret_cast<uint64>(boolean)) b* 5n == 0n)
    assert(0u * uint64 == 0u)
    assert(0n b* biguint == 0n)
    assert(true)
    assert(true)
    assert(0u * 3u == 0u)
    assert(0n b* 5n == 0n)
    assert(2u * uint64 == 6u)
    assert(2n b* biguint == 10n)
    assert(true)
    assert(true)
    assert(2u * 3u == 6u)
    assert(2n b* 5n == 10n)
    assert(uint64 * reinterpret_cast<uint64>(boolean) == 0u)
    assert(uint64 * uint64 == 9u)
    assert(itob(uint64) b* biguint == 15n)
    assert(uint64 * 0u == 0u)
    assert(uint64 * 2u == 6u)
    assert(uint64 * 3u == 9u)
    assert(itob(uint64) b* 5n == 15n)
    assert(3u * reinterpret_cast<uint64>(boolean) == 0u)
    assert(3u * uint64 == 9u)
    assert(itob(3u) b* biguint == 15n)
    assert(3u * 0u == 0u)
    assert(3u * 2u == 6u)
    assert(3u * 3u == 9u)
    assert(itob(3u) b* 5n == 15n)
    assert(biguint b* itob(reinterpret_cast<uint64>(boolean)) == itob(0u))
    assert(biguint b* itob(uint64) == itob(15u))
    assert(biguint b* biguint == 25n)
    assert(biguint b* 0n == 0n)
    assert(biguint b* 2n == 10n)
    assert(biguint b* itob(3u) == 15n)
    assert(biguint b* 5n == 25n)
    assert(5n b* itob(reinterpret_cast<uint64>(boolean)) == 0n)
    assert(5n b* itob(uint64) == 15n)
    assert(5n b* biguint == 25n)
    assert(5n b* 0n == 0n)
    assert(5n b* 2n == 10n)
    assert(5n b* itob(3u) == 15n)
    assert(5n b* 5n == 25n)
    return true
  }
  
  subroutine test_cases.numeric_upcasts.contract.BooleanBinaryOps.clear_state_program(): bool
  {
    return true
  }
  
  subroutine algopy._base_contract.BaseContract.__init__(): void
  {
  }
}