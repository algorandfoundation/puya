contract Issue194
{
  approval_program(): bool
  {
    assert(reinterpret_cast<bool>(1u) == reinterpret_cast<bool>(2u))
    two: uint64 = 2u
    switch (reinterpret_cast<bool>(two)) {
      case true: {
        return true
      }
      case _: {
        return false
      }
    }
  }
  
  clear_state_program(): bool
  {
    return true
  }
}