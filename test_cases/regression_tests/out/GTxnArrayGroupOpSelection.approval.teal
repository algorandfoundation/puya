#pragma version 11
#pragma typetrack false

// test_cases.regression_tests.op_variant_selection.GTxnArrayGroupOpSelection.approval_program() -> uint64:
main:
    // regression_tests/op_variant_selection.py:92-94
    // # T > 255, I <= 255 forces gtxnsas (both on stack) -> gtxnsa (I immediate, T on stack).
    // # Compile-only: runtime would fail since 256 is beyond the txn group.
    // assert op.GTxn.application_args(256, 0) != b""
    pushint 256
    gtxnsa ApplicationArgs 0
    pushbytes 0x
    !=
    assert
    // regression_tests/op_variant_selection.py:95
    // return True
    pushint 1
    return
