#pragma version 11
#pragma typetrack false

// test_cases.regression_tests.op_variant_selection.GloadIOpSelection.approval_program() -> uint64:
main:
    b main_block@0

main_block@0:
    // regression_tests/op_variant_selection.py:60-63
    // # I > 255 must leave gloadss alone: no AVM variant has T as immediate
    // # with I on the stack. Compile-only: runtime would fail since I is not
    // # a reachable scratch slot.
    // return op.gload_uint64(0, 256)
    pushint 0
    pushint 256
    gloadss
    return
