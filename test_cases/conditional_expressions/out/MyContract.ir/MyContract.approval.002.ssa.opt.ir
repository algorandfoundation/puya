main test_cases.conditional_expressions.contract.MyContract.approval_program:
    block@0: // L7
        (log "expensive_op")
        (log "side_effecting_op")
        (log "expensive_op")
        (log "side_effecting_op")
        (log "side_effecting_op")
        (log "expensive_op")
        let ternary_result%4#1: uint64 = 2u
        goto block@15
    block@15: // ternary_merge_L20
        let i#0: uint64 = ternary_result%4#1
        let result#0: uint64 = 19u
        let tmp%15#0: bytes = (itob result#0)
        (log tmp%15#0)
        return result#0