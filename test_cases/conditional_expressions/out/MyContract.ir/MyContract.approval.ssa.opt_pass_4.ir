main test_cases.conditional_expressions.contract.MyContract.approval_program:
    block@0: // L7
        (log "expensive_op")
        (log "side_effecting_op")
        (log "expensive_op")
        (log "side_effecting_op")
        (log "side_effecting_op")
        (log "expensive_op")
        let ternary_result%4#1: uint64 = 2u
        goto block@15
    block@15: // ternary_merge_L20
        let tmp%10#0: uint64 = 6u
        let tmp%11#0: uint64 = (* tmp%10#0 3u)
        let tmp%12#0: uint64 = (- tmp%11#0 1u)
        let result#0: uint64 = (+ tmp%12#0 ternary_result%4#1)
        let tmp%14#0: bytes = (itob result#0)
        (log tmp%14#0)
        return result#0