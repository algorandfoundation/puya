main test_cases.conditional_expressions.contract.MyContract.approval_program:
    block@0: // L7
        let tmp%0#0: bool = 1u
        let c#0: uint64 = (select 2u 1u tmp%0#0)
        let tmp%1#0: bool = 0u
        let d#0: uint64 = (select 1u 2u tmp%1#0)
        let tmp%0#8: bool = 1u
        (assert tmp%0#8) // Can't be 42
        (log "expensive_op")
        let tmp%2#0: bool = 0u
        goto tmp%2#0 ? block@1 : block@2
    block@1: // ternary_true_L14
        let ternary_result%0#0: uint64 = 0u
        goto block@3
    block@2: // ternary_false_L14
        let val#6: uint64 = 1u
        let tmp%0#7: bool = 1u
        (assert tmp%0#7) // Can't be 42
        (log "side_effecting_op")
        goto block@3
    block@3: // ternary_merge_L14
        let e#0: uint64 = φ(ternary_result%0#0 <- block@1, val#6 <- block@2)
        let tmp%0#6: bool = 1u
        (assert tmp%0#6) // Can't be 42
        (log "expensive_op")
        let tmp%3#0: bool = 1u
        goto tmp%3#0 ? block@4 : block@5
    block@4: // ternary_true_L15
        let ternary_result%1#0: uint64 = 3u
        goto block@6
    block@5: // ternary_false_L15
        let val#4: uint64 = 42u
        let tmp%0#5: bool = 0u
        (assert tmp%0#5) // Can't be 42
        (log "side_effecting_op")
        goto block@6
    block@6: // ternary_merge_L15
        let f#0: uint64 = φ(ternary_result%1#0 <- block@4, val#4 <- block@5)
        let tmp%0#4: bool = 1u
        (assert tmp%0#4) // Can't be 42
        (log "side_effecting_op")
        let tmp%4#0: bool = 1u
        goto tmp%4#0 ? block@7 : block@8
    block@7: // ternary_true_L17
        let ternary_result%2#0: uint64 = 0u
        goto block@9
    block@8: // ternary_false_L17
        let val#2: uint64 = 42u
        let tmp%0#3: bool = 0u
        (assert tmp%0#3) // Can't be 42
        (log "expensive_op")
        goto block@9
    block@9: // ternary_merge_L17
        let g#0: uint64 = φ(ternary_result%2#0 <- block@7, val#2 <- block@8)
        let tmp%0#2: bool = 1u
        (assert tmp%0#2) // Can't be 42
        (log "side_effecting_op")
        let tmp%5#0: bool = 0u
        goto tmp%5#0 ? block@10 : block@11
    block@10: // ternary_true_L18
        let ternary_result%3#0: uint64 = 2u
        goto block@12
    block@11: // ternary_false_L18
        let val#0: uint64 = 3u
        let tmp%0#1: bool = 1u
        (assert tmp%0#1) // Can't be 42
        (log "expensive_op")
        goto block@12
    block@12: // ternary_merge_L18
        let h#0: uint64 = φ(ternary_result%3#0 <- block@10, val#0 <- block@11)
        let tmp%6#0: bool = (< 2u c#0)
        goto tmp%6#0 ? block@13 : block@14
    block@13: // ternary_true_L20
        let ternary_result%4#0: uint64 = 1u
        goto block@15
    block@14: // ternary_false_L20
        let ternary_result%4#1: uint64 = (+ d#0 e#0)
        goto block@15
    block@15: // ternary_merge_L20
        let i#0: uint64 = φ(ternary_result%4#0 <- block@13, ternary_result%4#1 <- block@14)
        let tmp%7#0: uint64 = 2u
        let tmp%8#0: uint64 = (* tmp%7#0 c#0)
        let tmp%9#0: uint64 = (* tmp%8#0 d#0)
        let tmp%10#0: uint64 = (* tmp%9#0 f#0)
        let tmp%11#0: uint64 = (* tmp%10#0 h#0)
        let tmp%12#0: uint64 = (- tmp%11#0 e#0)
        let tmp%13#0: uint64 = (- tmp%12#0 g#0)
        let result#0: uint64 = (+ tmp%13#0 i#0)
        let tmp%14#0: bytes = (itob result#0)
        (log tmp%14#0)
        return result#0