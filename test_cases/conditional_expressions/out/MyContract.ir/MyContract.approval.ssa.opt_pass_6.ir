main test_cases.conditional_expressions.contract.MyContract.approval_program:
    block@0: // L7
        (log "expensive_op")
        (log "side_effecting_op")
        (log "expensive_op")
        (log "side_effecting_op")
        (log "side_effecting_op")
        (log "expensive_op")
        let tmp%12#0: uint64 = 17u
        let result#0: uint64 = (+ tmp%12#0 2u)
        let tmp%14#0: bytes = (itob result#0)
        (log tmp%14#0)
        return result#0