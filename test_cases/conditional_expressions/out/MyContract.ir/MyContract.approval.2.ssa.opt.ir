main test_cases.conditional_expressions.contract.MyContract.approval_program:
    block@0: // L7
        (log "expensive_op")
        (log "side_effecting_op")
        (log "expensive_op")
        (log "side_effecting_op")
        (log "side_effecting_op")
        (log "expensive_op")
        let ternary_result%4#1: uint64 = 2u
        let tmp%10#0: uint64 = 6u
        let tmp%11#0: uint64 = 18u
        let tmp%12#0: uint64 = 17u
        let tmp%13#0: uint64 = 17u
        let result#0: uint64 = 19u
        let tmp%14#0: bytes = (itob 19u)
        (log tmp%14#0)
        return 19u