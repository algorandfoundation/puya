main test_cases.state_proxies.contract.StateProxyContract.__algopy_entrypoint_with_init:
    block@0: // L1
        let lifted%return#0: bool = 1u
        let tmp%0#0: bool = (txn ApplicationID)
        goto tmp%0#0 ? block@2 : block@1
    block@1: // call __init___L1
        (app_global_put 0x6732 0u)
        let tmp%0#2: uint64 = (txn NumAppArgs)
        let funky_town#0: bytes = (select "town" "funky" tmp%0#2)
        (app_global_put funky_town#0 123u)
        goto block@2
    block@2: // after_if_else_L1
        let tmp%0#2: uint64 = (txn OnCompletion)
        goto tmp%0#2 ? block@9 : block@4
    block@4: // simple_routes_L17
        let tmp%2#0: uint64 = (txn ApplicationID)
        (assert tmp%2#0) // can only call when not creating
        let tmp%4#0: bytes = (txna ApplicationArgs 0) // on error: contract does not allow bare method calls
        switch tmp%4#0 {method "clear()void" => block@5, method "order_of_eval_global()void" => block@6, method "order_of_eval_local()void" => block@7, method "order_of_eval_box()void" => block@8, * => block@11}
    block@5: // clear_route_L37
        (app_global_del "g1")
        let tmp%0#4: account = (txn Sender)
        (app_local_del tmp%0#4 "l1")
        let tmp%1#0: account = (txn Sender)
        (app_local_del tmp%1#0 0x6c32)
        let materialized_values%0#0: bytes = test_cases.state_proxies.contract.get_key()
        let box_prefixed_key%0#0: bytes = (concat "box_map" materialized_values%0#0)
        (box_del box_prefixed_key%0#0)
        exit lifted%return#0
    block@6: // order_of_eval_global_route_L44
        let tmp%0#2: uint64 = test_cases.state_proxies.contract.default_value()
        let (maybe_value%0#1: uint64, maybe_exists%0#0: bool) = (app_global_get_ex 0u "g1")
        let val#0: uint64 = (select tmp%0#2 maybe_value%0#1 maybe_exists%0#0)
        let tmp%1#1: bool = (== val#0 42u)
        (assert tmp%1#1)
        exit lifted%return#0
    block@7: // order_of_eval_local_route_L49
        let tmp%0#4: account = test_cases.state_proxies.contract.get_account()
        let tmp%1#2: uint64 = test_cases.state_proxies.contract.default_value()
        let (maybe_value%0#1: uint64, maybe_exists%0#0: bool) = (app_local_get_ex tmp%0#4 0u "l1")
        let val#0: uint64 = (select tmp%1#2 maybe_value%0#1 maybe_exists%0#0)
        let tmp%2#1: bool = (== val#0 42u)
        (assert tmp%2#1)
        exit lifted%return#0
    block@8: // order_of_eval_box_route_L54
        let materialized_values%0#0: bytes = test_cases.state_proxies.contract.get_key()
        let box_prefixed_key%0#0: bytes = (concat "box_map" materialized_values%0#0)
        let tmp%0#2: uint64 = test_cases.state_proxies.contract.default_value()
        let (maybe_value%0#0: bytes, maybe_exists%0#0: bool) = (box_get box_prefixed_key%0#0)
        let maybe_value_converted%0#0: uint64 = (btoi maybe_value%0#0)
        let val#0: uint64 = (select tmp%0#2 maybe_value_converted%0#0 maybe_exists%0#0)
        let tmp%1#1: bool = (== val#0 42u)
        (assert tmp%1#1)
        exit lifted%return#0
    block@9: // other_routes_L17
        let tmp%5#0: bytes = (txna ApplicationArgs 0) // on error: contract does not allow bare method calls
        switch tmp%5#0 {method "create()void" => block@10, * => block@11}
    block@10: // create_route_L31
        let tmp%6#0: uint64 = (txn OnCompletion)
        let tmp%7#0: bool = (== tmp%6#0 OptIn)
        (assert tmp%7#0) // OnCompletion is not OptIn
        let tmp%8#0: uint64 = (txn ApplicationID)
        let tmp%9#0: bool = (! tmp%8#0)
        (assert tmp%9#0) // can only call when creating
        (app_global_put "g1" 1u)
        let tmp%0#4: account = (txn Sender)
        (app_local_put tmp%0#4 "l1" 2u)
        let tmp%1#0: account = (txn Sender)
        (app_local_put tmp%1#0 0x6c32 3u)
        exit lifted%return#0
    block@11: // after_if_else_L17
        fail

subroutine test_cases.state_proxies.contract.get_key() -> bytes:
    block@0: // L60
        (log "key")
        return 0x626f78

subroutine test_cases.state_proxies.contract.get_account() -> account:
    block@0: // L66
        (log "account")
        let tmp%0#0: account = (txn Sender)
        return tmp%0#0

subroutine test_cases.state_proxies.contract.default_value() -> uint64:
    block@0: // L72
        (log "default")
        return 42u