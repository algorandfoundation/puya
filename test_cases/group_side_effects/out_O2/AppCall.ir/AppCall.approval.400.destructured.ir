main algopy.arc4.ARC4Contract.approval_program:
    block@0: // L1
        let lifted%return#0: bool = 1u
        let tmp%0#1: uint64 = (txn NumAppArgs)
        goto tmp%0#1 ? block@2 : block@9
    block@2: // abi_routing_L4
        let tmp%2#0: bool = (txn ApplicationID)
        let tmp%3#0: bool = (! tmp%2#0)
        let tmp%4#0: uint64 = (txn OnCompletion)
        let tmp%5#0: uint64 = (shl 1u tmp%4#0)
        let tmp%6#0: uint64 = (+ tmp%3#0 tmp%5#0)
        let not%tmp%6#0: bool = (! tmp%6#0)
        (assert not%tmp%6#0)
        let tmp%7#0: bytes = (txna ApplicationArgs 0)
        switch tmp%7#0 {method "some_value()uint64" => block@4, * => block@13}
    block@4: // some_value_route_L5
        let tmp%0#1: uint64 = (global GroupSize)
        let tmp%1#2: uint64 = (txn GroupIndex)
        let tmp%2#2: uint64 = (+ tmp%1#2 1u)
        let tmp%3#1: uint64 = (* tmp%0#1 tmp%2#2)
        let aggregate%val_as_bytes%0#0: bytes[8] = (itob tmp%3#1)
        let tmp%2#1: bytes = (concat 0x151f7c75 aggregate%val_as_bytes%0#0)
        (log tmp%2#1)
        exit lifted%return#0
    block@9: // bare_routing_L4
        let tmp%8#0: uint64 = (txn OnCompletion)
        let not%tmp%8#0: bool = (! tmp%8#0)
        (assert not%tmp%8#0)
        let tmp%9#0: uint64 = (txn ApplicationID)
        let tmp%10#0: bool = (! tmp%9#0)
        (assert tmp%10#0) // can only call when creating
        exit lifted%return#0
    block@13: // after_if_else_L4
        fail