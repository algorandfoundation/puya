main test_cases.diamond_mro.gp.GP.__algopy_entrypoint_with_init:
    block@0: // L1
        let tmp%0#0: bool = (txn ApplicationID)
        goto tmp%0#0 ? block@2 : block@1
    block@1: // call __init___L1
        (log "gp.__init__")
        algopy._contract.Contract.__init__()
        goto block@2
    block@2: // after_if_else_L1
        let tmp%0#1: bool = test_cases.diamond_mro.gp.GP.__puya_arc4_router__()
        return tmp%0#1

subroutine test_cases.diamond_mro.gp.GP.__puya_arc4_router__() -> bool:
    block@0: // L6
        let tmp%0#0: uint64 = (txn OnCompletion)
        let tmp%1#0: bool = (! tmp%0#0)
        goto tmp%0#0 ? block@5 : block@1
    block@1: // simple_routes_L6
        let tmp%2#0: uint64 = (txn ApplicationID)
        let tmp%3#0: bool = (!= tmp%2#0 0u)
        (assert tmp%2#0) // can only call when not creating
        let tmp%4#0: bytes = (txna ApplicationArgs 0) // on error: contract does not allow bare method calls
        switch tmp%4#0 {method "method()void" => block@2, * => block@9}
    block@2: // method_route_L15
        test_cases.diamond_mro.gp.GP.method[routing]()
        exit 1u
    block@5: // other_routes_L6
        let tmp%5#0: bytes = (txna ApplicationArgs 0) // on error: contract does not allow bare method calls
        switch tmp%5#0 {method "create()void" => block@6, * => block@9}
    block@6: // create_route_L11
        let tmp%6#0: uint64 = (txn OnCompletion)
        let tmp%7#0: bool = (! tmp%6#0)
        (assert tmp%7#0) // OnCompletion is not NoOp
        let tmp%8#0: uint64 = (txn ApplicationID)
        let tmp%9#0: bool = (! tmp%8#0)
        (assert tmp%9#0) // can only call when creating
        test_cases.diamond_mro.gp.GP.create[routing]()
        exit 1u
    block@9: // after_if_else_L6
        fail

subroutine test_cases.diamond_mro.gp.GP.create[routing]() -> void:
    block@0: // L6
        return 

subroutine test_cases.diamond_mro.gp.GP.method[routing]() -> void:
    block@0: // L6
        (log "gp.method")
        return 

subroutine algopy._contract.Contract.__init__() -> void:
    block@0: // L1
        return 