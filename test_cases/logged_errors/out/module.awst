contract LoggedErrorsContract
{
  method_resolution_order: (
    algopy.arc4.ARC4Contract,
    algopy._contract.Contract,
  )
  
  subroutine algopy.arc4.ARC4Contract.approval_program(): bool
  {
    return arc4_router()
  }
  
  subroutine algopy.arc4.ARC4Contract.clear_state_program(): bool
  {
    return true
  }
  
  abimethod test_cases.logged_errors.contract.LoggedErrorsContract.test_logged_errs(arg: uint64): void
  {
    logged_assert(arg != 1u, comment="ERR:01")
    logged_assert(arg != 2u, comment="ERR:02:arg is two")
    logged_assert(arg != 3u, comment="AER:03")
    if (arg == 4u) {
      logged_err("ERR:04")
    }
  }
  
  baremethod test_cases.logged_errors.contract.LoggedErrorsContract.__algopy_default_create(): void
  {
  }
  
  subroutine algopy._contract.Contract.__init__(): void
  {
  }
}